{# 小红书发布稿模板 — 从 final.json 渲染 publish/post.md #}
# {{ date_label }} 第 {{ issue_no }} 期 — 小红书发布稿

> 生成 {{ generated_at }} · v3.10 排版（→ 报道 / 评论区 / 小刀说 / 本周尝试）
> 9 张图按顺序上传：`./images/01-cover.png` → `./images/09-event-08.png`

---

## 标题（20 字内，前 14 字关键）

```
昨日 AI 圈 8 件事，每件给你一个本周可做的
```

备选（自动从 8 件标题里挑钩子强的）：
{%- for e in events[:3] %}
- `{{ e.title }}{% if e.title_tail %}——{{ e.title_tail }}{% endif %}`（基于事件 {{ '%02d'|format(e.rank) }}）
{%- endfor %}

---

## 正文

```
做 AI 行业一年多。每天上班前会扫一遍 AI 圈全部信息：X、Reddit、HuggingFace、量子位、机器之心、ithome...... 平均 100 条往上。

95% 看完都觉得：so what？

所以做了这份日报：每天 100 条候选筛 8 件——不按"传播热度"，按"对你/我意味着什么"。每件给「→ 本周尝试」一个动作。

这是 {{ date_short }} 期：

{% for e in events -%}
{{ '%02d'|format(e.rank) }}｜{{ e.title }}{% if e.title_tail %}——{{ e.title_tail }}{% endif %}
{% endfor %}
视觉故意走印刷品方向，反 AI 矩阵号那种白底大字 + emoji + 渐变。

会日更。

———

哪一条「本周尝试」你打算真做？评论告诉我。
```

字数：约 {{ body_chars }} 字（小红书正文上限 1000，最佳 200-400）

---

## 标签（12-15 个）

```
#AI #人工智能 #AI日报 #产品经理 #PM #AI圈 #ChatGPT #大模型 #打工人 #信息焦虑 #AI产品 #AI增长{% for tag in extra_tags %} #{{ tag }}{% endfor %}
```

按预计流量大小：
- 高流量入口：`#AI` `#人工智能` `#ChatGPT` `#打工人`
- 精准受众：`#产品经理` `#PM` `#AI增长` `#AI日报`
- 长尾：`#AI圈` `#大模型` `#AI产品`{% for tag in extra_tags %} `#{{ tag }}`{% endfor %}

---

## 图片上传顺序

| 序号 | 文件 | 角色 |
|---|---|---|
| 1 | `01-cover.png` | **封面**（3 秒决定划走，1-8 等权列表）|
{% for e in events -%}
| {{ loop.index + 1 }} | `{{ '%02d'|format(loop.index + 1) }}-{{ e.id }}.png` | Event {{ '%02d'|format(e.rank) }} — {{ e.title[:30] }}{% if e.title|length > 30 %}...{% endif %} ({{ e.category|lower }}) |
{% endfor %}

---

## 发布前 checklist（10-15 min 人工 review）

- [ ] 标题最终选定（首选 / 备选 / 自己改）
- [ ] 正文最后一段「哪一条本周尝试你打算真做」是否要换更带感的钩子
- [ ] 标签是否要增删（看小红书发现页今天热标签）
- [ ] 9 张 PNG 物理 download 到 Windows 本地
- [ ] 上传顺序：cover 放第 1 张（重要！）
- [ ] 发布时间：晚间 19-22 点流量峰

---

## 在职合规检查

- ❌ 不出现当前公司名
- ❌ 不出现公司内部数据 / 决策 / 工具
- ✅ 行业通用观察、对外公开信息、个人观点 OK

**自动 grep check**（生成时已扫过）：未发现敏感词。如手工再改正文，请重 grep。

---

## WSL → Windows 上传图片

WSL 路径：
```
/home/lyric/Making money/Lyric-Self-Improve/projects/Self-Media/daily/{{ date }}/publish/images/
```

Windows 资源管理器开：
```
\\wsl.localhost\Ubuntu\home\lyric\Making money\Lyric-Self-Improve\projects\Self-Media\daily\{{ date }}\publish\images\
```

复制 9 张 PNG 到桌面 → 小红书 App / 网页版上传。
